while(true)
{
    cin >> input;
    if( ! isValid( input ))
    {
        continue;
    }
    //the continue here will skip to the very end of the while loop and then repeat it so that anything written here will be skipped when the above if loop is true
}
